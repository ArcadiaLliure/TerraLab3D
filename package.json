{
  "name": "terralab3d-workspace",
  "private": true,
  "scripts": {
    "typecheck": "tsc -p frontend/tsconfig.json --noEmit",
    "validate": "python tools/validate_skeleton.py && npm run typecheck"
  }
}
